apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

group = 'expo.modules.sharereceiver'
version = '0.1.0'

android {
    namespace 'expo.modules.sharereceiver'
    defaultConfig {
        versionCode 1
        versionName '0.1.0'
    }
}

expoModule {
    canBePublished false
}
